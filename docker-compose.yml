version: "3"

services:

  app:
    build: .

    ports:
      - "8000:8000"

  postgres:

    image: postgres:16

    environment:
      POSTGRES_DB: agentic_ai
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres